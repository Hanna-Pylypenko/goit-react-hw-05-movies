// import { useEffect, useState } from 'react';
// import { useParams } from 'react-router-dom';
export const MovieDetails = ({
  title,
  votes,
  overview,
  genres,
  cast,
  reviews,
  poster,
}) => {
  return <h1>{title}</h1>;
};
